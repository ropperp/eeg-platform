# Datenbank
DB_USER=eeg
DB_PASSWORD=ChangeMe!
DB_NAME=eeg_platform

# Domain (ohne https://)
DOMAIN=eegflow.at

# Let's Encrypt
ACME_EMAIL=patrick.ropper@gmail.com

# App
APP_SECRET=change-this-to-a-random-64-char-string

# latex-service
LATEX_API_KEY=change-this-api-key

# E-Mail (Brevo oder Postmark)
SMTP_HOST=smtp-relay.brevo.com
SMTP_USER=
SMTP_PASSWORD=

# Backup Dead-Man-Switch (healthchecks.io — kostenlos)
# Ping-URL von https://healthchecks.io kopieren, NICHT committen.
# backup.sh pingt diese URL nach jedem erfolgreichen Backup.
# Bleibt das Backup aus, schickt healthchecks.io eine E-Mail.
CHECK_URL=
