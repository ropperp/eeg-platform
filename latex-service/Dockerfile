# Platzhalter — Patrick liefert den fertigen latex-service Container
# Der echte Container läuft auf Port 3210 und hat bereits:
#   bezugsvereinbarung.tex und einspeisevereinbarung.tex
# Alexander ergänzt: rechnung.tex (bereits in templates/ vorhanden)

FROM node:20-alpine
WORKDIR /app
# TODO: Patrick liefert package.json + service.js
EXPOSE 3210
CMD ["echo", "latex-service: Bitte Dateien von Patrick einspielen"]
